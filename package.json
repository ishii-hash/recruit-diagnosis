{
  "name": "recruit-site-diagnosis",
  "version": "0.1.0",
  "private": true,
  "description": "採用サイト診断.ai — Cloudflare Pages + Functions + D1 + Claude API",
  "scripts": {
    "dev": "wrangler pages dev . --d1 DB=recruit-diagnosis",
    "deploy": "wrangler pages deploy .",
    "db:migrate": "wrangler d1 execute recruit-diagnosis --remote --file=schema.sql",
    "db:leads": "wrangler d1 execute recruit-diagnosis --remote --command=\"SELECT id, domain, url, total_score, grade, analyzed_at FROM analyses ORDER BY id DESC LIMIT 50\""
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  },
  "dependencies": {
    "jimp": "^1.6.1"
  }
}
